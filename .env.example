# Spring Application Configuration
SPRING_APPLICATION_NAME=dineease

# MySQL Database Configuration
MYSQL_HOST=localhost:3306/your_database_name
SPRING_DATASOURCE_USERNAME=your_mysql_username
SPRING_DATASOURCE_PASSWORD=your_mysql_password

# Server Configuration
SERVER_PORT=8080

# JWT Configuration
# Generate a secure JWT secret key (minimum 256 bits / 64 characters)
# You can generate one using: openssl rand -base64 64
JWT_SECRET_KEY=your_jwt_secret_key_here_minimum_256_bits_recommended
# Token expiration in milliseconds (86400000 = 1 day)
JWT_EXPIRATION_MS=86400000

# Docker Configuration
# Format: dockerhub-username/image-name
DOCKER_IMAGE_NAME=your-dockerhub-username/your-image-name

# NOTE: Docker Hub credentials should NEVER be stored in .env files
# For CI/CD, add these as GitHub Secrets:
# - DOCKER_USERNAME (your Docker Hub username or email)
# - DOCKER_TOKEN (your Docker Hub access token)